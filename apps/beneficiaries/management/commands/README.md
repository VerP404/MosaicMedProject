# 🔧 Management команды для beneficiaries

Этот каталог содержит управляющие команды Django для работы с приложением льготников.

## 📚 Доступные команды

### 1. `create_beneficiaries_testdata`

Создание тестовых данных для демонстрации и тестирования системы.

**Использование:**
```bash
python manage.py create_beneficiaries_testdata
```

**Создаёт:**
- 4 категории льгот (федеральная, региональная, детская, орфанные)
- 5 лекарственных препаратов
- 5 пациентов с различными диагнозами
- 7 назначений препаратов с разными статусами
- Остатки препаратов на складе

**Когда использовать:**
- При первоначальной настройке системы
- Для демонстрации функционала
- При разработке и тестировании

---

### 2. `sync_from_recipes`

Импорт данных из таблицы `Recipe` (приложение load_data).

**Использование:**
```bash
# Полная синхронизация всех рецептов
python manage.py sync_from_recipes

# Ограничить количество обрабатываемых рецептов
python manage.py sync_from_recipes --limit 100

# Тестовый запуск без сохранения в БД
python manage.py sync_from_recipes --dry-run

# Комбинация параметров
python manage.py sync_from_recipes --limit 50 --dry-run
```

**Параметры:**
- `--limit N` - обработать только N первых рецептов
- `--dry-run` - тестовый режим без записи в БД

**Что синхронизирует:**
1. Категории льгот из рецептов
2. Лекарственные препараты
3. Пациентов-льготников (по СНИЛС)
4. Назначения препаратов пациентам

**Когда использовать:**
- При начальной загрузке данных из существующих рецептов
- Для периодической синхронизации новых данных
- При миграции данных из других систем

---

### 3. `update_beneficiaries_data` ⭐ НОВАЯ

Регулярное обновление и актуализация данных льготников.

**Основное использование:**
```bash
# Выполнить все операции обновления
python manage.py update_beneficiaries_data --all

# Тестовый запуск
python manage.py update_beneficiaries_data --all --dry-run
```

**Отдельные операции:**

#### Обновление статусов назначений
```bash
python manage.py update_beneficiaries_data --update-statuses
```
Проверяет даты обеспечения и автоматически обновляет статусы:
- Истекшие назначения → `expired`
- Срочные (≤7 дней) → остаются `active` с предупреждением

#### Проверка истекших назначений
```bash
python manage.py update_beneficiaries_data --check-expired
```
Находит и отмечает все истекшие назначения, выводит статистику по срочным.

#### Деактивация старых записей
```bash
# По умолчанию - записи старше 180 дней
python manage.py update_beneficiaries_data --deactivate-old

# Настройка порога (например, 90 дней)
python manage.py update_beneficiaries_data --deactivate-old --days 90
```
Находит и деактивирует:
- Старые истекшие назначения
- Пациентов без активных назначений

**Параметры:**
- `--all` - выполнить все операции
- `--update-statuses` - обновить статусы назначений
- `--check-expired` - проверить истекшие назначения
- `--deactivate-old` - деактивировать старые записи
- `--days N` - порог для старых записей (дней)
- `--dry-run` - тестовый режим

**Выводимая статистика:**
- Количество активных пациентов, препаратов, категорий
- Распределение назначений по статусам
- Топ-5 назначаемых препаратов
- Количество срочных назначений
- Препараты с низкими остатками на складе

**Когда использовать:**
- **Ежедневно** - автоматическое обновление через cron/планировщик
- **Еженедельно** - проверка и актуализация данных
- **После импорта** - нормализация статусов после загрузки данных
- **Перед отчётами** - актуализация перед формированием отчётов

---

## 🤖 Автоматизация

### Linux/Mac (cron)

Добавьте в crontab:
```bash
# Редактировать crontab
crontab -e

# Обновление данных каждый день в 2:00
0 2 * * * cd /path/to/MosaicMedProject && source .venv/bin/activate && python manage.py update_beneficiaries_data --all

# Синхронизация из рецептов каждую неделю по воскресеньям в 3:00
0 3 * * 0 cd /path/to/MosaicMedProject && source .venv/bin/activate && python manage.py sync_from_recipes
```

### Windows (Task Scheduler)

**PowerShell команда для создания задачи:**
```powershell
# Создание ежедневной задачи обновления в 2:00
schtasks /create /tn "UpdateBeneficiaries" `
  /tr "powershell -Command 'cd D:\Projects\MosaicMedProject; .venv\Scripts\Activate.ps1; python manage.py update_beneficiaries_data --all'" `
  /sc daily /st 02:00

# Создание еженедельной синхронизации по воскресеньям в 3:00
schtasks /create /tn "SyncBeneficiaries" `
  /tr "powershell -Command 'cd D:\Projects\MosaicMedProject; .venv\Scripts\Activate.ps1; python manage.py sync_from_recipes'" `
  /sc weekly /d SUN /st 03:00
```

**Или через GUI:**
1. Откройте "Планировщик заданий" (Task Scheduler)
2. Создайте новую задачу
3. Триггер: Ежедневно в 2:00
4. Действие: Запустить программу
   - Программа: `powershell.exe`
   - Аргументы: `-Command "cd D:\Projects\MosaicMedProject; .venv\Scripts\Activate.ps1; python manage.py update_beneficiaries_data --all"`

---

## 📊 Примеры сценариев использования

### Сценарий 1: Начальная настройка системы
```bash
# 1. Создать тестовые данные для проверки
python manage.py create_beneficiaries_testdata

# 2. Проверить работу обновления
python manage.py update_beneficiaries_data --all --dry-run

# 3. Если всё ок, импортировать реальные данные
python manage.py sync_from_recipes

# 4. Обновить статусы после импорта
python manage.py update_beneficiaries_data --all
```

### Сценарий 2: Регулярное обслуживание
```bash
# Утром каждый день (автоматически)
python manage.py update_beneficiaries_data --all

# После получения новых рецептов
python manage.py sync_from_recipes --limit 1000
python manage.py update_beneficiaries_data --update-statuses
```

### Сценарий 3: Подготовка отчёта
```bash
# Перед формированием отчёта актуализировать данные
python manage.py update_beneficiaries_data --all

# Проверить статистику
python manage.py update_beneficiaries_data --check-expired
```

### Сценарий 4: Очистка старых данных
```bash
# Проверить что будет удалено (dry-run)
python manage.py update_beneficiaries_data --deactivate-old --days 365 --dry-run

# Выполнить деактивацию записей старше года
python manage.py update_beneficiaries_data --deactivate-old --days 365
```

---

## 🔍 Отладка

### Проверка что команда работает
```bash
python manage.py update_beneficiaries_data --help
```

### Тестирование без изменений
```bash
python manage.py update_beneficiaries_data --all --dry-run
```

### Подробный вывод
```bash
python manage.py update_beneficiaries_data --all --verbosity 2
```

### Логирование
Команды используют стандартное логирование Django. Настройте в `settings.py`:
```python
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': 'beneficiaries_commands.log',
        },
    },
    'loggers': {
        'apps.beneficiaries.management': {
            'handlers': ['file'],
            'level': 'INFO',
            'propagate': True,
        },
    },
}
```

---

## 📝 Заметки

- Все команды безопасны и не удаляют данные без явного указания
- Используйте `--dry-run` для предварительной проверки
- Команда `update_beneficiaries_data` идемпотентна - безопасно запускать многократно
- При больших объёмах данных используйте `--limit` для постепенной обработки

---

## 🆘 Часто задаваемые вопросы

**Q: Можно ли запускать команды параллельно?**
A: Да, но избегайте одновременного запуска команд, которые изменяют одни и те же данные.

**Q: Что делать если команда упала с ошибкой?**
A: Проверьте логи, используйте `--verbosity 2` для подробного вывода, запустите с `--dry-run`.

**Q: Как часто нужно запускать update_beneficiaries_data?**
A: Рекомендуется ежедневно. Минимум - раз в неделю для актуальности данных.

**Q: Безопасно ли запускать --all?**
A: Да, команда не удаляет данные, только обновляет статусы и деактивирует неактуальные записи.

